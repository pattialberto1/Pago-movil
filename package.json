{
  "name": "pago-movil",
  "private": true,
  "scripts": {
    "build": "npm ci --include=dev --prefix frontend && npm run build --prefix frontend && npm ci --include=dev --prefix backend && npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "engines": {
    "node": ">=22"
  }
}
